# Lead capture (Estimator + Contact forms → /api/lead → Resend).
# Same Resend account + verified sender (caroluxinsulation.com) as carolux-tools.
# Local: copy to .env.local with the real key. Production: add in the Vercel
# project's Environment Variables, then redeploy. Without it, /api/lead returns 503.
RESEND_API_KEY=
